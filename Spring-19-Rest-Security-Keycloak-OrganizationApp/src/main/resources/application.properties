server.port=8082

spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=

spring.jpa.show-sql=false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.sql.init.mode=always

spring.jpa.hibernate.ddl-auto=create

spring.jpa.defer-datasource-initialization=true

spring.jackson.serialization.fail-on-empty-beans=false

# connecting application to keycloak

#telling the realm name
keycloak.realm=apidev
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.ssl-required=external
#resource means client name
keycloak.resource=atapi
keycloak.credentials.secret=ftvjnMhlm1axHbvI3uoBXv5UXZQOYShM
#restricting access through client/role level
keycloak.use-resource-role-mappings=true
#restricting access through bearer type
keycloak.bearer-only=true
spring.main.allow-circular-references=true

